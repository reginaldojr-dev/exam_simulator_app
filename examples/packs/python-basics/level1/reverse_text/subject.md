Implemente `reverse_text(text)` retornando a string invertida.

## Arquivo esperado

`reverse_text.py`

## Comportamento esperado
- Assinatura obrigatória: `reverse_text(...)`.
- Retorne o valor; o harness do app serializa em JSON.
- Não escreva código de execução no topo do módulo.

## Permitido
- `funções`
- `estruturas nativas`

## Não permitido
- `print dentro da função`
