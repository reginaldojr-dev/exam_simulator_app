Assignment name  : reverse_text
Expected files   : reverse_text.py
Function         : def reverse_text(text)
--------------------------------------------------------------------------------

Write a function `reverse_text` that receives a string and returns the same
string reversed.

Examples:

reverse_text("abc") -> "cba"
reverse_text("a")   -> "a"
