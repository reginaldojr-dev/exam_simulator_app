Implemente `safe_divide(a, b)` retornando `None` quando houver divisão por zero.

## Arquivo esperado

`safe_divide.py`

## Comportamento esperado
- Assinatura obrigatória: `safe_divide(...)`.
- Retorne o valor; o harness do app serializa em JSON.
- Não escreva código de execução no topo do módulo.

## Permitido
- `funções`
- `estruturas nativas`

## Não permitido
- `print dentro da função`
